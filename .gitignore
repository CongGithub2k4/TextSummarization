# Thư mục dữ liệu

# Một số file dữ liệu lớn cụ thể nếu chúng vẫn đang bị tracking ngầm
# (Chỉ thêm nếu bạn vẫn thấy chúng sau khi đã thêm cả thư mục)
data/processed/
data/processed/

# Notebook checkpoints (file tạm của Jupyter)
.ipynb_checkpoints/

# Cấu hình môi trường ảo (nếu bạn không muốn push venv)
venv/